import os
from PyQt5.QtGui import QPixmap
from PyQt5.QtWidgets import QDialog
from [[ python_package_import_name ]].settings import *


class AboutDialog(QDialog):
    def __init__(self, *args, **kwargs):
        super(AboutDialog, self).__init__(*args, **kwargs)
        ui_loader("about.ui", self)
        self.about_app_logo.setPixmap(
            QPixmap(os.path.join(app_icon_path, 'logo.png')))
        self.about_version_value.setText(app_settings.value('version'))
        # self.about_license_type_value.setText(app_settings.value('license_type'))
        self.show()